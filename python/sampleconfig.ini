[PYTHON_PATH]
PATH = /usr/local/bin/python3.4

[TOKEN]
consumerKey = fillmein
consumerSecret = fillmein
devAccessToken = fill only if dev.else delete
devAccessSecret = fill only if dev. else delete

[SECRETKEY]
secretKey = fillmein
